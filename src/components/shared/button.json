{
  "collectionName": "components_shared_buttons",
  "info": {
    "displayName": "button",
    "icon": "play",
    "description": ""
  },
  "options": {},
  "attributes": {
    "theme": {
      "type": "enumeration",
      "default": "primary",
      "enum": [
        "primary",
        "accent",
        "warn"
      ]
    },
    "link": {
      "type": "component",
      "conditions": {
        "visible": {
          "!=": [
            {
              "var": "action"
            },
            "request-a-call"
          ]
        }
      },
      "component": "shared.link",
      "repeatable": false
    },
    "type": {
      "type": "enumeration",
      "default": "filled",
      "enum": [
        "elevated",
        "filled",
        "filled_tonal",
        "outlined",
        "text",
        "icon",
        "segmented",
        "fab",
        "fab_extended"
      ]
    },
    "icon": {
      "type": "string"
    },
    "action": {
      "type": "enumeration",
      "default": "navigate",
      "enum": [
        "navigate",
        "request-a-call"
      ]
    }
  }
}
